{
  "name": "iojs_tls_bug",
  "version": "0.0.1",
  "engines": {
    "iojs": ">= 1"
  },
  "scripts": {
    "test": "mocha --expose-gc --reporter spec -- index.js"
  },
  "dependencies": {
    "memwatch": "marcominetti/node-memwatch",
    "mocha": "^2.1.0"
  }
}
